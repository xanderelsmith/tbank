import 'package:toronet/toronet.dart'; void main() async { final sdk = ToronetSDK(network: Network.testnet, baseUrl: 'https://testnet.toronet.org/api'); try { final res = await sdk.tnsService.getAddress(name: 'bob'); print('BOB RESOLVED: ' + res.toString()); } catch(e) { print('BOB ERROR: ' + e.toString()); } }
